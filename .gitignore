node_modules/

# Local secrets — the README tells you to create .env.local for `vercel dev`,
# and it holds the live Stripe secret and the Google service-account key.
# .env.example is the only one that belongs in git.
.env
.env.*
!.env.example

# Playwright test output
playwright-report/
test-results/

# Agent work artifacts — generated at runtime, not tracked
.claude/work/**/*.md

# Lighthouse CI local reports
.lighthouseci/
